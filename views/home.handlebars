<h3>Where have I been?</h3>
<form action="/" method="POST">
    <input type="hidden" name="latitude" value=""/>
    <input type="hidden" name="longitude" value=""/>
	<input type="hidden" name="map" value=""/>
    <button type="submit" class="btn btn-primary btn-lg" disabled>Check In!</button>
</form>

{{! below the form, above the script tag }}


{{#if error}}
<p class="text-warning">{{error}}</p>
{{/if}}
{{#if location}}
<p>
    The browser says you are at <strong>{{location.latitude}}, {{location.longitude}}</strong>.
</p>
<p>
    Your Map Data appears to be:
    <blockquote>{{location.map}}</blockquote>
</p>
{{/if}}

<script>
//Handlebars.registerHelper('eachProperty', function(context, options) {
//    var ret = "";
//    for(var prop in context)
//    {
//        ret = ret + options.fn({property:prop,value:context[prop]});
//    }
//    return ret;
//});
</script>

{{#if bizData}}
<p>
    Restaurants close to you that are open
</p>

<div id="restaurantSection">
	{{#bizData}}
		<div class="restaurant {{#categories}}{{/categories}}">
			<h2><a href="{{url}}">{{name}}</a><br/></h2>
			<img src="{{image_url}}"/><br/>
		    <p>Categories: {{categories}}</p>
			<img src="{{rating_img_url_small}}"/><br/>
			<br/>
			<a href="{{url}}">
				<button type="button" class="btn btn-primary btn-sm">Check out their site!</button>
			</a>
		</div>
	{{/bizData }}
</div>

{{/if}}



<!--{{#if restaurantData}}-->
<!--<p>-->
    <!--Here are some places that you can try out-->
<!--</p>-->
<!--<ul>-->
	<!--{{#restaurantData.businesses}}-->
		<!--<li> {{ name }}: {{ mobile_url }} </li>-->
	<!--{{/restaurantData.businesses}}-->
<!--</ul>-->
<!--{{/if}}-->

<script src="javascripts/client.js"></script>